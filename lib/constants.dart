class AppConstants {
  // Prevent instantiation
  AppConstants._();

  // Constraints
  static const double maxUploadSizeInMB = 10.0;
  static const int maxFileCount = 5;
  static const double defaultPadding = 16.0;

  static const isLogin = "isLogin";

}